import styled from 'styled-components/macro'
import { background, shadow, size } from '../../src'

export const Shadow = styled.h1`
${size.height.medium}
  ${background.color.black}
  ${shadow.large}
`
